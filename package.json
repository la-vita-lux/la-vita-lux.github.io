{
  "name": "la-vita-lux-github-io-root",
  "private": true,
  "scripts": {
    "build": "cd source && npm ci --no-audit --no-fund && npm run build:publish",
    "dev": "cd source && npm run dev",
    "preview": "cd source && npm run preview"
  }
}

